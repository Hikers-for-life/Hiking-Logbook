import { Navigation } from "../components/ui/navigation";

const Index = () => {
  return (
    <div className="min-h-screen bg-background">
      <Navigation />
      { /* We add the rest when we have the full home page */ }
    </div>
  );
};

export default Index;